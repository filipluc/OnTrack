# Leave unset for local dev (requests go through Vite's proxy to localhost:4000).
# Set to the deployed backend's origin (no trailing slash) for production builds,
# e.g. https://ontrack-api.onrender.com
VITE_API_BASE_URL=
